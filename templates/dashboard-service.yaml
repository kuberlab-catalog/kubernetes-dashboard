apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard
  namespace: kube-system
spec:
  type: {{ .Values.serviceType }}
  selector:
    k8s-app: kubernetes-dashboard
  ports:
  - port: 80
    protocol: TCP
    name: HTTP
    targetPort: 9090
